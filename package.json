{
  "name": "vat-checker",
  "version": "1.0.0",
  "description": "Shopify Plus VAT Checker app with VIES validation",
  "private": true,
  "scripts": {
    "dev": "shopify app dev",
    "deploy": "shopify app deploy"
  },
  "workspaces": [
    "web",
    "extensions/*"
  ],
  "engines": {
    "node": ">=20.0.0"
  }
}

